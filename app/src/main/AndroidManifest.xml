<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.encuestando.salmeron.facundo.encuestandofcm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pfc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MenuNormalActivity" />
        <activity android:name=".MenuEspecialActivity" />
        <activity android:name=".InfoNoticiasNormalActivity" />
        <activity android:name=".WebViewNormalActivity" />
        <activity
            android:name=".InfoNoticiasEspecialActivity"
            android:label="@string/title_activity_info_noticias_especial"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".CrearInfoActivity" />
        <activity android:name=".AdministrarEncuestasActivity" />
        <activity android:name=".NuevaEncuestaActivity"></activity>
    </application>

</manifest>